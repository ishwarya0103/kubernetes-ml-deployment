apiVersion: v1
kind: Service
metadata:
  name: cifar10-service
  labels:
    app: cifar10
spec:
  type: NodePort
  selector:
    app: cifar10
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007