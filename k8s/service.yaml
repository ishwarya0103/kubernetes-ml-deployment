apiVersion: v1
kind: Service
metadata:
  name: cifar10-service
spec:
  type: NodePort
  selector:
    app: cifar10
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007